This project is a simple RESTful User API built with Express.js. It allows basic CRUD operations including creating, reading, updating, and deleting users. The data is stored in memory, so it resets whenever the server restarts. This project is ideal for learning and practicing backend development concepts such as routing, request handling, status codes, and JSON responses.

Use attendance record
